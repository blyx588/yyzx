<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.yyzx.mapper.CheckoutMapper">

    <select id="selectCheckoutVo" resultType="com.neuedu.yyzx.vo.CheckoutVo">
        SELECT
            o.id,
            o.is_deleted,
            c.customer_name,
            o.customer_id,
            o.checkout_type,
            o.checkout_reason,
            o.checkout_time,
            o.audit_status
        FROM
            checkout o LEFT JOIN customer c ON ( o.customer_id = c.id )
    </select>
</mapper>
