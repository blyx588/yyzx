<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.yyzx.mapper.OutwardMapper">

    <select id="selectOutwardVo" resultType="com.neuedu.yyzx.vo.OutwardVo">
        SELECT
            o.id,
            o.is_deleted,
            c.customer_name,
            o.customer_id,
            o.outgoing_reason,
            o.outgoing_time,
            o.expected_return_time,
            o.actual_return_time,
            o.escorted,
            o.relation,
            o.escorted_tel,
            o.audit_status
        FROM
            outward o LEFT JOIN customer c ON ( o.customer_id = c.id )
    </select>
</mapper>
